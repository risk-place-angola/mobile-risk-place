# Miscellaneous
*.env

*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/

# Web related
lib/generated_plugin_registrant.dart

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Firebase
ios/Runner/GoogleService-Info.plist
android/app/google-services.json
lib/firebase_options.dart

# Env
.env
.env.local
.env.*.local
*.keystore
*.jks
key.properties

# CI/CD Credentials (não commitar!)
service-account-*.json
google-services-dev.json
google-services-prod.json
google-services-staging.json
firebase-credentials.json
*-credentials.json

# FVM Version Cache
.fvm/

# iOS Build artifacts (already covered by /build/ but more explicit)
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
ios/build/

# iOS specific - Firebase config should be ignored (already listed above)
# ios/Runner/GoogleService-Info.plist

# iOS Certificates and Provisioning (não commitar!)
*.p12
*.cer
*.mobileprovision
*.certSigningRequest
ios/ExportOptions.plist.local
ios/Runner.xcodeproj/xcuserdata/
ios/Runner.xcworkspace/xcuserdata/

# iOS Private Keys (APNs, App Store Connect)
AuthKey_*.p8
*.p8

# Android local config (already covered)
# android/local.properties

# Build directories (more specific)
build/
android/build/
ios/build/

*.pem